# Backend Portfolio Tools Dependencies (Port 8000)
# Only includes packages actually used in the code

# Core FastAPI and async support
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Pydantic and settings
pydantic==2.5.0
python-dotenv==1.0.0

# OpenAI integration (older version for compatibility)
openai==1.3.7

# File processing
python-multipart==0.0.6

# HTTP client
httpx<0.25.0
httpcore<1.0.0 